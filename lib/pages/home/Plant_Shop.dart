import 'package:flutter/material.dart';

class Plant_Shop extends StatelessWidget {
  const Plant_Shop({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: const Color.fromARGB(255, 165, 247, 169),
      child: const Center(
        child: Text(
          "Plant shop",
        ),
      ),
    );
  }
}
